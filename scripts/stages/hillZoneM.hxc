import flixel.FlxG;
import funkin.play.stage.Stage;

class HillZoneMStage extends Stage
{
	public function new()
	{
		super('hillZoneM');
	}

    override function buildStage()
	{
		super.buildStage();

		if (FlxG.onMobile) {
			getNamedProp('cyclesMontain').scale.set(0.65, 0.5);
		}
	}
}
